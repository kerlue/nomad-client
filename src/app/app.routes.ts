import { Routes } from '@angular/router';
import { AppFrontendLayout } from './layouts/app-frontend-layout';

export const routes: Routes = [
  {
    path: '',
    component: AppFrontendLayout
  }
];
